export default {
    getProfile(state) {
        return state.profile
    },

    getRoles(state) {
        return state.roles
    },

    isAuthenticated(state) {
        return state.isAuthenticated
    }
}
